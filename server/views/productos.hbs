{{> head }}
    {{> navbar }}

    <pre>
        {{productos}}
        <input type="text" name="productos" id="productos" value="{{imagen}}">
        <img src="images/productos/{{imagen}}" alt="">
        
    </pre>

    <script>
        $(document).ready(() => {
            const productosJson = $('#productos')[0].value;
            console.log(productosJson);
        })
    </script>

{{> footer}}